export const metadata = {
  title: "Search Console",
  description: "Access the recovered footage vault by movie title, category, vibe, archive flags, and score filters.",
};

export default function SearchLayout({ children }) {
  return children;
}